# Polymarket API client - pinned to specific version for stability
py-clob-client==0.34.5

# WebSocket client for market data (CLOB WSS)
websockets==12.0

# HTTP client for market discovery and price fetching
httpx==0.27.0

# Environment configuration
python-dotenv==1.0.1

# Rich console output (optional, falls back to basic logging if not available)
rich==13.7.1

# Web3 for on-chain balance verification
web3>=6.20.1

# Required by py-clob-client
# eip712-structs-ng==2.0.1
# eth-keys==0.4.0

# Testing dependencies
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-asyncio==0.21.1

# Infrastructure dependencies (Week 2)
redis==5.0.1                    # Redis client for state/events
structlog==24.1.0               # Structured logging
tenacity==8.2.3                 # Retry logic with exponential backoff
pydantic==2.5.3                 # Configuration management
pydantic-settings==2.1.0        # Environment variable loading

# WebSocket server dependencies (Week 3)
python-socketio==5.14.0         # Socket.IO server for real-time events
aiohttp==3.13.3                  # Async HTTP server for Socket.IO
asyncio==4.0.0